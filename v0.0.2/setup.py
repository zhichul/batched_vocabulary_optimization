from setuptools import setup, find_packages

setup(
    name='bopt',
    author='Brian Lu',
    version='0.0.2',
    description='Batched Vocab Optimization',
    packages=["bopt"],
    package_dir={"":"src"}
)
